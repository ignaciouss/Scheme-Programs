(modulo 142 100)
(truncate 12394 2000)

(define (mixTwo x y)
  (cond (((< x 0) #f)
         ((< y 0) #f))


        (let ((fI (modulo x 100))
              (fS

              ))
        )


        )
  )

